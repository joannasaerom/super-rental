import Ember from 'ember';

export default Ember.Component.extend({
  // getLatestAnnouncement: function(announcements){
  //   var latest = Math.max.apply(null, announcements)
  //   return latest;
  // }
});
